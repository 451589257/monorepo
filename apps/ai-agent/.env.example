# 通道：openai | claude | gemini（缺省 openai）
PROVIDER=openai

# 密钥。三家的 key 不通用，切通道时要一并换掉
API_KEY=sk-xxx

# 自定义端点，可指向 DeepSeek / Moonshot / 本地 Ollama 等。
# 只有 openai 通道读它，claude / gemini 会忽略
BASE_URL=https://api.openai.com/v1

# 模型名，留空则用各通道自己的默认模型
MODEL=gpt-4o-mini

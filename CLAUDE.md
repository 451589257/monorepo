# 团队协作规范

## 项目上下文获取

`CLAUDE.md` 只负责协作与执行约束，不承载完整项目事实。

当任务需要项目背景、模块边界、技术栈、运行/验证命令或局部编码约束时，先按需读取 [README.md](/Users/jacky/Node/monorepo/README.md) 的相关章节，通过 README 定位具体文档或源码位置。

不要默认预加载或通读 README、docs 目录或无关模块文档。执行具体任务时，以目标代码、配置文件和测试作为主要上下文来源；README 和 docs 只作为索引与辅助说明。

若 README/docs 与代码或配置不一致，以代码和配置为准，并在回复中指出文档可能过期。

## 一、编码前思考

**不做假设。不隐藏困惑。呈现权衡。**

- 存在歧义时，明确列出多种解释，询问而不是猜测
- 有更简单的方案时，主动说出来，不默默执行
- 需求不清或方案存在明显分歧时，先厘清意图、边界与影响，再动手
- 合理假设必须明确标注

## 二、简洁优先

**用最少的代码解决问题。不过度推测。**

- 不添加需求之外的功能、灵活性或可配置性
- 不为一次性代码创建抽象
- 不为不可能发生的场景做防御处理
- **检验标准：** 资深工程师会觉得这过于复杂吗？如果是，简化。

## 三、精准修改

**只碰必须碰的。只清理自己造成的混乱。**

- 只做当前任务所需的最小必要改动，避免无关重构
- 不"改进"相邻代码、注释或格式
- 匹配现有命名、模块边界和代码风格
- 注意到无关死代码时：提一下，不要删除
- 自己改动产生的孤儿导入/变量/函数：清理掉
- **检验标准：** 每一行修改都应能直接追溯到用户的请求

## 四、目标驱动执行

**定义成功标准。循环验证直到达成。**

将指令式任务转化为可验证的目标：

| 不要这样   | 转化为                               |
| ---------- | ------------------------------------ |
| "添加验证" | "为无效输入编写测试，然后让它们通过" |
| "修复 bug" | "编写重现 bug 的测试，然后让它通过"  |
| "重构 X"   | "确保重构前后测试都通过"             |

多步骤任务说明简短计划：

```
1. [步骤] → 验证: [检查]
2. [步骤] → 验证: [检查]
```

修改后给出验证结果；若未验证，明确说明原因与剩余假设。

## 五、协作规范

- 所有回复使用中文
- 修改代码前必须先读取并理解仓库上下文
- 回复简洁、行动导向，按可交付步骤逐一推进
- 涉及 UI 改动，必须先完成本地预览确认，再交付
- 不得泄露凭据，不得执行危险操作，遵循安全规范
